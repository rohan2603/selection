name: demo

on:
  push:
    paths:
    - 'cicd/**'
  pull_request:
    paths: 
    - 'cicd/**'

jobs:
  build:
  # defaults:
  #   run:
  #     working-directory: ./cicd
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "hello"

