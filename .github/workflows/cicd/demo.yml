name: demo

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  path:
    ./cicd

jobs:
  build:
  # defaults:
  #   run:
  #     working-directory: ./cicd
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo "hello"

