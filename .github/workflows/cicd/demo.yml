name: demo

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
  # defaults:
  #   run:
  #     working-directory: ./cicd
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          repository: rohan2603/selection
          path: cicd

      - run: echo "hello"
        working-directory: ./cicd

