name: Validating All Yaml Files
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  validation:
    runs-on: ubuntu-latest
    steps:
      - name: my-step-1
        run: echo "workflow 1"