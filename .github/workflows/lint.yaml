---
name: workflow1
on:
  workflow_run:
    workflows: ["yamllint"]
    types:
      - completed
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step-1
        run: echo "workflow 1"