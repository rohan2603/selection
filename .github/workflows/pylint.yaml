name: Sonarqube
on:
  workflow_run:
    workflows: ["Validating"]
    types:
      - completed
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: my-step-3
        run: echo "workflow3"


        