---

name: Python Linting
on:
  workflow_run:
    workflows: ["Sonarqube"]
    types:
      - completed
jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: my-step-2
        run: echo "workflow2"

        