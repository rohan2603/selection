---
name: sonarqube
on:
  workflow_run:
    workflows: [Pylint]
    types:
      - requested
      - completed
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step-3
        run: echo "sonarqube"