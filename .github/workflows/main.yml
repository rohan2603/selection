
name: Mixed inputs

on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Name of GCP resource to be triggered
        options: 
        - Bucket
        - Cloud Function
      region:
        required: true

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Send greeting
      run: echo "${{ github.event.inputs.region }} ${{ fromJSON('["", "🥳"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"
